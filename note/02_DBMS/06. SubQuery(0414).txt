◎ Sub Query
1. 단일행 서브쿼리
	: 서브쿼리의 결과가 단일 행
		* 서브쿼리 결과 -> 메인쿼리
		* 메인쿼리의 WHERE절 <- =, >, >=, <, <=, <> 사용

2. 다중행 서브쿼리 : 서브쿼리의 결과가 2행 이상
	: 서브쿼리의 결과가 2행 이상
		* 반드시 다중행 연산자와 함께 사용
		* 다중행 연산자의 종류
			①IN : 하나 이상 일치
			②ALY, SOME : 하나 이상 일치
			③ALL : 모든 값이 일치
			④EXISTS : 만족하는 값이 하나 이상 존재
